<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Broadcast Upload Extension Info.plist. NSExtension keys tell the OS this is a
	     "com.apple.broadcast-services-upload" extension point, backed by SampleHandler,
	     and that it participates in the system's live broadcast (not just recording). -->

	<key>CFBundleIdentifier</key>
	<string>io.bheka.agent.BhekaBroadcastExtension</string>

	<key>CFBundleExecutable</key>
	<string>BhekaBroadcastExtension</string>

	<key>CFBundleName</key>
	<string>BhekaBroadcastExtension</string>

	<key>CFBundlePackageType</key>
	<string>XPC!</string>

	<key>CFBundleDisplayName</key>
	<string>Bheka Monitoring</string>

	<key>CFBundleShortVersionString</key>
	<string>1.0.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>

	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.broadcast-services-upload</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).SampleHandler</string>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- RPBroadcastProcessMode "sample buffer" mode delivers frames directly to
			     this extension's processSampleBuffer(_:with:), which is what we need
			     for real-time OCR + upload. The alternative, "broadcast to a URL", is
			     for saving raw video and does not apply here. -->
			<key>RPBroadcastProcessMode</key>
			<string>RPBroadcastProcessModeSampleBuffer</string>
		</dict>
	</dict>

	<!-- App Transport Security exception mirrored from the main app's Info.plist so the
	     extension can also reach the on-prem Bheka server over HTTP. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<false/>
		<key>NSExceptionDomains</key>
		<dict>
			<key>100.87.148.94</key>
			<dict>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
				<key>NSIncludesSubdomains</key>
				<false/>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
